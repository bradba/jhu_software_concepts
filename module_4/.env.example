# Database Configuration
# Option 1: Use DATABASE_URL (PostgreSQL connection string)
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Option 2: Use individual parameters (if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bradleyballinger
DB_USER=bradleyballinger
# DB_PASSWORD=your_password_here

# LLM API Configuration
LLM_API_URL=http://localhost:8000/standardize
