# Graduate School Applicant Database Analysis System
# Python Dependencies

# Database
psycopg[binary]>=3.1.0

# Web Framework
Flask>=3.0.0

# PDF Generation
reportlab>=4.0.0

# Web Scraping
beautifulsoup4>=4.12.0
urllib3==2.6.3  # Pinned to fix High severity vulnerabilities (SNYK scan 2026-02)

# Additional dependencies (typically already included)
Pillow==12.1.1  # Pinned to fix High severity Out-of-bounds Write (SNYK-PYTHON-PILLOW-15265439)
charset-normalizer>=3.0.0  # Required by reportlab

# Security patches for indirect dependencies
jinja2==3.1.6  # Pinned to fix XSS/Template Injection (Flask dependency)
requests==2.32.4  # Pinned to fix sensitive info leakage (Sphinx dependency)
certifi==2024.7.4  # Pinned to fix verification issues

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# Documentation
sphinx>=7.2.0
sphinx-rtd-theme>=2.0.0

# Code Quality & Analysis Tools
pylint>=3.0.0
pydeps>=3.0.0
