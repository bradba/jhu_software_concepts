# Database Connection Configuration
# Copy this file to .env and replace with your actual values
# NEVER commit .env to version control

# Option 1: Use DATABASE_URL (recommended for production/Heroku/cloud platforms)
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Option 2: Use individual environment variables (recommended for local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=gradcafe_app_user
DB_PASSWORD=your_secure_password_here

# Notes:
# - DB_USER should be a least-privilege user (see README.md for setup instructions)
# - Use a strong password (minimum 16 characters, mix of letters/numbers/symbols)
# - The database user only needs SELECT, INSERT permissions on the applicants table
# - Never commit real credentials to git
